<style>
    .signup__link {
        text-decoration: none;
        color: black;
        font-size: 17px;
        font-weight: bold;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }

    .signup__link span {
        color: #48507B;
    }
</style>
   
<script>
   async function handleLogin() {
        console.log('Tring to login ........')
        const email = document.getElementById('email').value
        const password = document.getElementById('password').value
        const showErrorMessage = false
       const formData = new FormData()
       formData.append('emailAddress', email)
       formData.append( "password", password)

       try {
            const response = await axios.put('/api/v1/entrance/login' , formData)
            document.location = '/post'
       } catch(error) {
            console.log('user can not login', error.toString())
       }
    }
</script>

<div class="signin__form" style="display: flex; justify-content: space-between; height: 610px; width: 1024px; background: white; border-radius: 30px;">
    <div style="height: 610px; width: 380px;">
        <img style="height: 610px; width: 380px;" src="/images/logn-cover-image.png" alt="logn-cover-image">
    </div>
    <div style=" width: 640px; text-align: center; padding: 162px;">
        <h1 style="font-size: 48px; font-family: SF Pro Display; text-align: left;">Sign In</h1>
        <form action="/api/v1/entrance/login" method="POST" style="display: grid; grid-template-rows: repeat(3, 1fr); grid-row-gap: 20px;">
            <input id="email" name="emailAddress" type="text" placeholder="Email" style="border: 1px solid #DBE1FF; outline: none; height: 50px; border-radius: 5px; padding-left: 5px;">
            <input id="password" name="password" type="text" placeholder="Password" style="border: 1px solid #DBE1FF; outline: none; height: 50px; border-radius: 5px; padding-left: 5px;">
            <button type="button" onclick="handleLogin()" style="border: none; height: 50px; border-radius: 5px; font-size: 20px; font-weight: bold;">Login</button>
            <a href="" class="signup__link">Do not have a Account? <span>SignUp</span></a>
        </form>
    </div>
    
</div>